[기능] Code Documentation (코드 문서화)
[길이] Medium (중간 길이)
[복잡도] Basic (기본)
[설명] 기본적인 CRUD 서비스의 Javadoc 생성

다음 UserService 클래스에 대한 Javadoc을 생성해줘:
```java
public class UserService {
    private final Map<String, User> users = new HashMap<>();
    
    public void createUser(String id, String name, String email) {
        if (users.containsKey(id)) {
            throw new IllegalArgumentException("User already exists");
        }
        users.put(id, new User(id, name, email));
    }
    
    public User getUser(String id) {
        User user = users.get(id);
        if (user == null) {
            throw new IllegalArgumentException("User not found");
        }
        return user;
    }
    
    public void updateUser(String id, String name, String email) {
        User user = getUser(id);
        user.setName(name);
        user.setEmail(email);
    }
    
    public void deleteUser(String id) {
        if (!users.containsKey(id)) {
            throw new IllegalArgumentException("User not found");
        }
        users.remove(id);
    }
} 