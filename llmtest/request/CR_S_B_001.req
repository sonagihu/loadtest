[기능] Code Review (코드 리뷰)
[길이] Short (짧은 요청)
[복잡도] Basic (기본)
[설명] 간단한 유틸리티 메소드의 코드 리뷰

다음 유틸리티 메소드에 대한 코드 리뷰를 작성해줘:
```java
public static int calculateSum(int[] numbers) {
    int sum = 0;
    for (int i = 0; i <= numbers.length; i++) {
        sum += numbers[i];
    }
    return sum;
}
``` 